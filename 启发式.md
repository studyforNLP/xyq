[baselines.py](E:/xyq/code/baselines.py) 目前实现了 6 个确定性启发式基线和 1 个纯 rollout 启发式基线 RH。

**整体调度框架**
所有 baseline 都走同一个 `build_baseline_solution()`：

1. 初始化环境。
2. 每个时间步先处理零时长任务。
3. 调用 `generate_available_actions()` 得到当前可执行动作。
4. 根据不同规则选出一个“动作集”，不是只选单个动作。
5. 用 `is_action_conflict()` 去掉冲突动作，保证同一时间步内动作不互斥。
6. 执行动作集，推进正在运行任务。
7. 最后用 `-calculate_global_reward()` 得到总延误惩罚 `penalty`。

对应代码在 [baselines.py](E:/xyq/code/baselines.py):280。

**各启发式规则**
规则核心在 `_rule_key()`，代码通过 `min(key)` 选择排序最靠前的动作，因此负号表示“越大越优”。

`MPV-SLK-EFT`：最大优先级 + 最小松弛 + 最早完成  
排序键是：

```python
(-priority, slack, finish_time, task_id, person_id, action_order)
```

含义是先选优先级最高的任务，再选时间松弛 `slack` 最小的任务，最后选预计完成时间最早的人员/动作。代码在 [baselines.py](E:/xyq/code/baselines.py):123。

`MXS+MF`：最大后继任务数 + 最早完成  
排序键是：

```python
(-successor_count, -priority, slack, finish_time, duration, task_id, person_id, action_order)
```

这里 `successor_count` 是传递后继数量，不只是直接后继。也就是说，越处在项目网络关键上游、影响后续任务越多的任务越优先。之后再按优先级、松弛时间、完成时间和持续时间打破平局。代码在 [baselines.py](E:/xyq/code/baselines.py):125。

需要注意：它不是严格“两阶段：先选任务，再选人员”的写法，而是对“任务-人员动作”统一排序。但由于 `successor_count` 排在第一位，效果上仍然体现“优先处理后继任务数最多的任务”。

`SLK-EFT`：最小松弛 + 最早完成  
排序键是：

```python
(slack, finish_time, -priority, task_id, person_id, action_order)
```

优先选 `time_difference` 最小的任务，也就是越紧迫越优先；若紧迫程度相同，则选择预计完成时间更早的动作。代码在 [baselines.py](E:/xyq/code/baselines.py):137。

`SPT`：最短处理时间  
排序键是：

```python
(duration, finish_time, slack, -priority, task_id, person_id, action_order)
```

优先选择当前人员执行时间最短的动作。对于 `assign` 动作，duration 来自该任务在该人员上的加工时间；对于 `resume/continue`，duration 是剩余时间。代码在 [baselines.py](E:/xyq/code/baselines.py):139。

`ECT`：最早完成时间  
排序键是：

```python
(finish_time, duration, slack, -priority, task_id, person_id, action_order)
```

优先选择预计完成时间最早的动作，即 `current_time + duration` 最小。它和 SPT 的区别是：SPT只看处理时间短，ECT看从当前时刻算起谁最早完成。代码在 [baselines.py](E:/xyq/code/baselines.py):141。

`FIFO`：先到先服务  
FIFO 会给第一次出现在 `available_actions` 中的任务记录顺序 `fifo_order`，之后优先选择更早进入可选集合的任务。

排序键是：

```python
(fifo_order, finish_time, duration, task_id, person_id, action_order)
```

也就是说，先按进入可调度队列的先后顺序，再按完成时间和持续时间打破平局。代码在 [baselines.py](E:/xyq/code/baselines.py):143。

**RH 规则**
`RH` 是纯启发式 rollout baseline，不训练 Q 表。它不使用固定优先级规则，而是用 `calculate_action_set_cost_to_go()` 评估候选动作集的后续代价。

当 `rollout_beam_width <= 1` 时，它是贪心 rollout：

1. 从当前可行动作中试探加入一个动作。
2. 对 `selected_actions + [action]` 计算 cost-to-go。
3. 选择 cost-to-go 最小的动作。
4. 删除与该动作冲突的候选动作。
5. 重复直到无法继续加入动作。

代码在 [baselines.py](E:/xyq/code/baselines.py):220。

当 `rollout_beam_width > 1` 时，它使用 beam search：

1. 从空动作集开始。
2. 扩展所有不冲突候选动作。
3. 用 cost-to-go 对扩展后的动作集排序。
4. 每层只保留前 `beam_width` 个动作集。
5. 最后选择 cost-to-go 最小的完整动作集。

这相当于“无 Q-learning 的 rollout 构解器”，用于和 RQL 区分：RH 只靠前瞻启发式评估，RQL 则先训练 Q 表，再结合 Q 表和 cost-to-go 构解。

**一个实现细节**
`RANDOM_METHODS = {"QL", "RH", "RQL"}`，所以实验脚本会让 RH 也按多个 seed 运行。但从 [baselines.py](E:/xyq/code/baselines.py) 本身看，RH 没有显式随机采样，主要是确定性 cost-to-go 排序。因此如果 cost-to-go 内部也没有随机性，同一个案例下 RH 的不同 seed 结果应当相同。